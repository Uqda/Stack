# UQDA Stack v0.1.0

Official initial release promotion from tested `main` commit `fa89f20ba10b53881bff2d1ca33c5ba6ab330aa4`.

The release workflow must build all declared platforms, generate SHA-256 checksums, sign the manifest with GitHub OIDC/Sigstore, create provenance attestations, and publish only after every job succeeds.
